# NativeThink 开发日志

## 2026-07-11

### 性能优化 🚀
- **网页加载速度**: 全部页面改为 `React.lazy` + Suspense 懒加载，主包 **2.3MB → 897KB (-61%)**
- **Dashboard 优化**: `MOCK_CHUNKS` (146KB) 改为动态 `import()`，首页不再捆绑整个语块库
- **词库引擎重构**: 移除冗余 `cleanWordEntry` 映射，预建 `_levelIndex` 索引，`getAllWords()` 惰性缓存
- **同步缓存检查**: `loadLevel()` 同步检查 localStorage 命中则立即返回，二次访问秒开
- **搜索优化**: `useDeferredValue` 延迟搜索，输入不阻塞 UI 渲染
- **后台预加载**: 引导向导挂载时立即在后台加载全部词库，消除"开始学习"后的等待

### 词书 & 学习流程 📚
- 所有 tab 统一使用 wizard 选定的词书，移除独立等级选择器
- 引导流程重构：选词书 → 选模式 → 学习量/复习方式
- 每日学习量扩展：预设 5/10/20/30/50/100 + 自定义输入框 (1-9999)
- **学习 & 复习统一 6 种模式**：闪卡 / 选择题 / 拼写 / 听写 / 配对 / 填空
- 学习 tab 解除 simple 限制，复习 tab 改用 DailyLearningMode，两 tab 均支持全部模式
- **页内一键切换模式**，无需返回引导向导，每种模式独立配色
- `reviewFocus` 属性区分标题（"学习模式" vs "复习模式"，"每日目标" vs "复习目标"）
- **切换词书按钮**：页面标题旁新增醒目词书切换卡片，可随时更换词书和模式
- 词书/模式记忆功能，刷新自动恢复

### 朗读引擎 (TTS) 🔊
- **多引擎架构**: SpeechSynthesis (主) → Edge WebSocket TTS (备) → Google TTS (兜底)
- 修复 Chromium `cancel()` 竞态条件导致后续 `speak()` 静默失败
- 添加 start-timeout 备用检测：引擎不触发 onstart 时自动降级
- 自动语音选择 → 朗读时用可选列表匹配降级
- 手机端 TTS 声音选择器修复 (Popover modal + Select viewport)

### 移动端适配 📱
- 底部 Tab 导航栏
- 响应式布局 (`col-span-12 lg:col-span-X`)
- 移动端间距优化
- 词库浏览筛选芯片紧凑布局
- 手机端 TTS 声音选择修复

### UI 改进 ✨
- 词库浏览 & 搭配学习双列布局，统一界面风格
- 词库浏览添加词数汇总 + 筛选芯片（词性/语域/情感/有搭配词/中文无对应/乱序）
- 左/右列 `lg:sticky` 置顶，滚动时保持可见
- 词列表分页管理，页码可跳转
- 語块训练语块库分页 + AI 生成另页
- 搭配学习添加上一词/下一词导航
- 每日一句收藏双向切换 + toast 反馈
- **切换词书按钮突出显示**：渐变卡片 + 词书图标 + 当前等级名称，hover 增强

---

## 2026-07-10

### 初始功能
- 手机版适配基础布局
- 词汇页加载骨架屏
- AI 双语输出 (英文 + 中文)
- Cloudflare Pages 部署支持
- 每日一句收藏和历史记录修复
