# API routes — pass through (don't rewrite)
/api/* /api/* 200
# 内置模型目录（打包版同源加载；web 版未内置 → 404 让 transformers 走远程）
/models/* /models/* 404
# SPA fallback — all other routes serve index.html
/* /index.html 200
