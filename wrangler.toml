name = "nativethink"
compatibility_date = "2024-12-01"
pages_build_output_dir = "dist/client"

# Cloudflare Pages configuration
# Deploy with: npx wrangler pages deploy
# Or via GitHub Actions (see .github/workflows/deploy-cf.yml)

[vars]
NODE_ENV = "production"
